# export MY_REGION=$(curl -s http://169.254.169.254/latest/dynamic/instance-identity/document | jq -r .region)
# eksctl create cluster -f team1-cluster.yaml --region $MY_REGION

eksctl create cluster \
  --name 변수처리 \
  --region $MY_REGION \
  --vpc-private-subnets subnet-변수처리 \
  --node-type t3.medium \
  --nodes 2 \
  --managed \
  --node-private-networking
